## Run multiple containers using a built image

Running this is exactly the same as the previous example. The differences are only in the implementation. Here the image is built with the ssh stack included using a Dockerfile that is invoked via docker compose.
